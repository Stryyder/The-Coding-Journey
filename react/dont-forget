// don't forget how things are passed. E.G. this example how the component is passed to the renderToString method


class App extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <div/>
  }
};

// change code below this line
ReactDOMServer.renderToString(<App />);
